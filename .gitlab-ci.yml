stages:
  - deploy

deploy:
  stage: deploy
  tags:
    - docker
  script:
    - cd Plantarium
    - ls -l nginx.conf        # Debug
    - docker-compose down
    - docker-compose build
    - docker-compose up -d