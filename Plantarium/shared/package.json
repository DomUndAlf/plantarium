{
  "name": "@plantarium/shared-prisma",
  "version": "1.0.0",
  "main": "prismaClient.ts",
  "dependencies": {
    "@prisma/client": "^6.15.0"
  },
  "devDependencies": {
    "prisma": "^6.15.0"
  },
  "scripts": {
    "migrate": "prisma migrate dev --schema=./prisma/schema.prisma",
    "generate": "prisma generate --schema=./prisma/schema.prisma"
  }
}
